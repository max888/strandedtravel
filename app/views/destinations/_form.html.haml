= form_for(@destination) do |f|
  - if @destination.errors.any?
    #error_explanation
      %h2
        = pluralize(@destination.errors.count, "error")
        prohibited this destination from being saved:
      %ul
        - @destination.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :city
    %br/
    = f.text_field :city
  .field
    = f.label :country
    %br/
    = f.text_field :country
  .field
    = f.label :description
    %br/
    = f.text_area :description

  %p#note
    For best results use the largest version of an instagram photo. You can get this by grabbing a screen shot of the picture at
    %a{:href => "http://instagram.com"} instagram.com
    (For Macs Command+Shift+4, PC's use the snipping tool in start menu). This should save to your desktop where you can upload.

    Sometimes it is easier to search with
    %a{:href => "http://statigr.am/"} Statigr.am

  .field
    = f.label :photo
    %br/
    = f.file_field :photo

  .actions
    = f.submit